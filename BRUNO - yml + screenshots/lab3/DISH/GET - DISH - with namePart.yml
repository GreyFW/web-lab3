info:
  name: GET - DISH - with namePart
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/dishes?namePart=Ice"
  params:
    - name: namePart
      value: Ice
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
