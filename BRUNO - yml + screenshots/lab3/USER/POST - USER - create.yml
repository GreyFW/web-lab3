info:
  name: POST - USER - create
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/v1/users"
  body:
    type: json
    data: |
      {
        "email": "for_deletion@gmail.com",
        "firstName": "Delete-Me",
        "lastName": "Please",
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
