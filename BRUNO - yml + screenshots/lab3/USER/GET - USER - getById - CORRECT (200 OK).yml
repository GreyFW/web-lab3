info:
  name: GET - USER - getById - CORRECT (200 OK)
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
