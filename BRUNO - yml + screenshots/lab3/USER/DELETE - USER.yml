info:
  name: "DELETE - USER "
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/api/v1/users/99"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
