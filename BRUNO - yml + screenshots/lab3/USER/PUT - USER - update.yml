info:
  name: PUT - USER - update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/api/v1/users/2"
  body:
    type: json
    data: |
      {
        "email": "random_email@gmail.com",
        "firstName": "Who-am-I",
        "lastName": "I-dont-know",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
